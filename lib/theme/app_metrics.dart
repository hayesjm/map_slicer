class AppMetrics {
  // ================================
  // TitledPanel (titled_panel.dart)
  // ================================
  static const panelRadius = 5.0;
  static const panelBodyPadding = 5.0;

  static const panelHeaderHorizontalPadding = 5.0;
  static const panelHeaderVerticalPadding = 5.0;
  static const panelHeaderFontSize = 14.0;
  static const panelHeaderLetterSpacing = 1.15;

  // ====================================
  // Workspace layout (workspace_shell.dart)
  // ====================================
  static const workspaceGap = 8.0;
  static const leftColumnWidth = 320.0;

  // ====================================
  // Compact stacked panels (left_control_column.dart)
  // ====================================
  static const compactPanelTopPadding = 8.0;

  // ====================================
  // SliderNumberRow control (slider_number_row.dart)
  // ====================================
  static const controlGap = 5.0;
  static const labelButtonRadius = 4.0;
  static const controlFontSize = 13.0;
  static const numberFieldVerticalPadding = 5.0;
  static const controlRowVerticalPadding = 5.0;
  static const labelWidth = 70.0;
  static const labelHeight = 28.0;
  static const numberFieldWidth = 45.0;
  static const unitsWidth = 12.0;
  static const unitsGap = 5.0;

  static const infoRowVerticalPadding = 0.0;
  // ====================================
  // Bottom information panel (right_workspace.dart)
  // ====================================
  static const infoRowHeight = 270.0;
}
